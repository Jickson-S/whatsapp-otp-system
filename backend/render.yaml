services:
  - type: web
    name: whatsapp-bot-system
    env: node

    buildCommand: |
      apt-get update
      apt-get install -y chromium
      npm install

    startCommand: node server.js

    envVars:
      - key: CHROME_PATH
        value: /usr/bin/chromium

    plan: free